[pytest]
# importlib mode resolves the `tests` namespace collision that arises
# when both analytics/tests/ and api/tests/ are collected together —
# both directories are __init__.py packages named "tests", which
# causes a ModuleNotFoundError in the default prepend/append import modes.
addopts = --import-mode=importlib
