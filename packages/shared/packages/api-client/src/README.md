# packages/api-client/src/

Placeholder for the TypeScript API client package.

## Purpose

Provides a typed HTTP client for the SATOR API, consuming `packages/stats-schema`
types. Used by the web platform (`apps/sator-web`) to interact with the API.

## Planned Exports

- `SatorApiClient` — Main client class
- `PlayerStats` — Re-exported from `stats-schema`
- `MatchResult` — Re-exported from `stats-schema`

## Status

Phase 1 placeholder — implementation begins in Phase 4.
